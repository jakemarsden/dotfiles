- shell:
  - command: git submodule update --init --recursive
    description: Update submodules

- clean:
    ~/:
    ~/.config:
      recursive: true

- defaults:
    link:
      create: true
      relink: true

- link:
    ~/.config/git/:
      path: config/git/*
      glob: true
